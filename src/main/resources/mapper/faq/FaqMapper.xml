<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skix.datapreprocess.faq.mapper.FaqMapper">

    <insert id="upsertFaqs">
        INSERT INTO faq (
            large_category, middle_category, small_category, title, body, processed_text, mobile_body, use_yn,
            source_file_name, source_row_no
        ) VALUES
        <foreach collection="faqs" item="faq" separator=",">
            (
                #{faq.largeCategory}, #{faq.middleCategory}, #{faq.smallCategory}, #{faq.title},
                #{faq.body}, #{faq.processedText}, #{faq.mobileBody}, #{faq.useYn},
                #{faq.sourceFileName}, #{faq.sourceRowNo}
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
            large_category = VALUES(large_category),
            middle_category = VALUES(middle_category),
            small_category = VALUES(small_category),
            title = VALUES(title),
            body = VALUES(body),
            processed_text = VALUES(processed_text),
            mobile_body = VALUES(mobile_body),
            use_yn = VALUES(use_yn)
    </insert>
</mapper>
